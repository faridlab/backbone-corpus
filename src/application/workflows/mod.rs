//! Workflow orchestration (saga/flow patterns) — intentionally empty.
//!
//! The scaffolded example saga that used to live here was removed: it was
//! generator scaffolding with no references anywhere outside this module.
//! The module file itself is kept (and declared under `user_owned` in
//! metaphor.codegen.yaml) because the generated `src/application/mod.rs`
//! declares `pub mod workflows;` unconditionally.
